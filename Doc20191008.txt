Ориентироваца на лекции "Технострим Mail.Ru Group".
+GitHub установлен и настроен,
	использование изучено до начального уровня.
+Maven установлен, изучена начальная логика применения.
-Определить шаблон Maven проекта для Android приложения,
	расположить в git репозитории.
-Определить IDE разработки Android приложений на Linux.
-Когда же уже понадобица установка Java SDK ? )))

Проверка синхронизации.
Кодировка UTF-8.
Конец строки Unix.
!!!Git не понимает русские имена файлов!!!

Клонирование репозитория на локальный диск:
git clone адрес

Показать разницу между облаком и локальной версией:
git status

Добавить новые файлы к отслеживаемым/включить в список передаваемых в репозиторий:
git add файл
git add -A
git add --all

Подготовка к передаче в облако(репозиторий) наших изменений:
git commit -m "сообщение описывающее что синхронизируется с облаком, какие изменения, состояние разработки"

Отправка изменений в облако:
git push

Синхронизация с облаком/получение свежей версии из облака на локальный диск:
git pull
